{
  "name": "@sentinel/voice-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda handler that drives Amazon Connect contact flows and routes calls",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../function.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-connect": "^3.490.0",
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/client-sfn": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0",
    "@sentinel/schemas": "*",
    "@sentinel/types": "*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "typescript": "*"
  }
}
