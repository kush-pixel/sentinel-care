{
  "name": "@sentinel/summarizer",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda that transcribes call recordings and produces Bedrock-powered clinical summaries",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../function.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.490.0",
    "@aws-sdk/client-s3": "^3.490.0",
    "@aws-sdk/client-transcribe": "^3.490.0",
    "@sentinel/schemas": "*",
    "@sentinel/types": "*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "typescript": "*"
  }
}
